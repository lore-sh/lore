{
  "name": "toss",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@tsconfig/bun": "^1.0.10",
    "@types/bun": "^1.3.3"
  },
  "scripts": {
    "toss": "bun run ./packages/cli/src/main.ts",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test"
  }
}
