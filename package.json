{
  "name": "toss",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/bun": "^1.3.3"
  },
  "scripts": {
    "toss": "bun run --cwd packages/cli src/main.ts",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test"
  }
}
