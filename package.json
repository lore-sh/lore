{
  "name": "lore",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@tsconfig/bun": "^1.0.10",
    "@types/bun": "^1.3.3"
  },
  "scripts": {
    "lore": "bun run ./packages/cli/src/main.ts",
    "typecheck": "bun run --workspaces typecheck",
    "test": "bun test",
    "test:ci": "bun test"
  }
}
