{
  "name": "@lore/web",
  "version": "0.1.0",
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "astro": "^5.17.1",
    "react": "^19.2.4",
    "satori": "^0.19.2"
  },
  "private": true,
  "scripts": {
    "prepare:install": "../../scripts/web/generate-install.sh",
    "dev": "bun run prepare:install && astro dev",
    "build": "bun run prepare:install && astro build",
    "preview": "bun run prepare:install && astro preview",
    "astro": "astro"
  },
  "type": "module",
  "devDependencies": {
    "@types/react": "^19.2.14"
  }
}
